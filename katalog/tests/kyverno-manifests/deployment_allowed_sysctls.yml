# Copyright (c) 2017-present SIGHUP s.r.l All rights reserved.
# Use of this source code is governed by a BSD-style
# license that can be found in the LICENSE file.

---
# Allowed by restrict-sysctls: kernel.shm_rmid_forced is in the allowed list.
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: deployment-allowed-sysctls
  name: deployment-allowed-sysctls
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      run: deployment-allowed-sysctls
  template:
    metadata:
      labels:
        run: deployment-allowed-sysctls
    spec:
      securityContext:
        sysctls:
          - name: kernel.shm_rmid_forced
            value: "1"
      containers:
        - image: registry.sighup.io/fury/redis:5.0.8
          name: deployment-allowed-sysctls
          livenessProbe:
            httpGet:
              path: /healthy
              port: 8080
          readinessProbe:
            httpGet:
              path: /healthy
              port: 8080
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            runAsUser: 1001
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              memory: "60Mi"
              cpu: "30m"
            limits:
              memory: "80Mi"
              cpu: "50m"
